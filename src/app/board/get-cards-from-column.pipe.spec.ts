import { GetCardsFromColumnPipe } from './get-cards-from-column.pipe';

describe('GetCardsFromColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCardsFromColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
